# PolicyExplainer – copy to .env and fill in values. Never commit .env.

# Required: OpenAI API key (no default; must be non-empty)
OPENAI_API_KEY=

# Optional: model names (safe defaults used if unset)
EMBEDDING_MODEL=text-embedding-3-small
LLM_MODEL=gpt-4o-mini

# Optional: Chroma persistence directory (default: ./chroma_data; use empty for in-memory)
VECTOR_DB_PATH=./chroma_data
