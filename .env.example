# PolicyExplainer Environment Configuration Template

# --------------------------------------------------
# REQUIRED SETTINGS
# --------------------------------------------------
# OpenAI API key used for embeddings and LLM responses.
OPENAI_API_KEY=your_key_here

# --------------------------------------------------
# OPTIONAL SETTINGS (Defaults will be used if not set)
# --------------------------------------------------
# Embedding model used to convert policy text into vector representations.
EMBEDDING_MODEL=text-embedding-3-small

# LLM model used for generating summaries and answering questions.
LLM_MODEL=gpt-4o-mini

# Directory for storing the local Chroma vector database.
# Leave empty ("") to use temporary in-memory storage.
VECTOR_DB_PATH=./chroma_data
